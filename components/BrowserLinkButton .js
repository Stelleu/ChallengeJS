import Component from "../core/Components";

export default class BrowserLinkButton extends Button {
    constructor(props) {
      super(props);
      this.name = "browser-link-button";
    }
    
  }